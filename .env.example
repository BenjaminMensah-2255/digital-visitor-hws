# PostgreSQL connection used by Prisma. Replace values for your environment.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/digital_visitors?schema=public&connect_timeout=15"

# Use true behind HTTPS in production so the session cookie is Secure.
SESSION_COOKIE_SECURE="false"
